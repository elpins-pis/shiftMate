# 본인 근무만 보기 수정 내용

## 적용 내용

- 관리자(`ADMIN`)는 기존처럼 전체 직원과 전체 근무표를 볼 수 있습니다.
- 일반 사용자(`USER`)는 로그인 계정과 연결된 직원 1명의 근무만 달력과 통계에서 볼 수 있습니다.
- 참여 요청 승인 화면에서 관리자가 요청자를 기존 직원명과 연결한 뒤 승인하도록 변경했습니다.
- 연결된 직원이 없는 일반 사용자는 달력 대신 “연결된 직원 정보가 없습니다” 안내가 표시됩니다.
- Supabase RLS 정책도 함께 강화할 수 있도록 `supabase/link_employee_accounts.sql` 파일을 추가했습니다.

## 수정한 주요 파일

- `src/services/workspaceService.js`
  - 로그인 사용자의 workspace member `user_id`를 읽습니다.
  - `employees.user_id`와 로그인 사용자 ID가 같은 직원만 일반 사용자에게 내려줍니다.
  - 일반 사용자의 스케줄 조회는 해당 직원 ID로 제한합니다.

- `src/router/AppRouter.jsx`
  - 참여 요청 승인 시 직원 ID를 함께 넘기도록 수정했습니다.
  - 설정 화면에 직원 목록을 전달합니다.

- `src/pages/SettingsPage.jsx`
  - 참여 요청 승인 카드에 “연결할 직원 선택” 셀렉트를 추가했습니다.
  - 직원을 선택하지 않으면 승인되지 않도록 막았습니다.

- `src/pages/CalendarPage.jsx`
  - 일반 사용자에게 연결된 직원이 없을 때 안내 화면을 표시합니다.

- `supabase/schema.sql`
  - 신규 설치용 스키마에 `employees.user_id`와 관련 정책을 반영했습니다.

- `supabase/link_employee_accounts.sql`
  - 기존 Supabase DB에 적용할 마이그레이션 SQL입니다.

## 기존 DB를 쓰는 경우 꼭 해야 할 일

Supabase SQL Editor에서 아래 파일 내용을 먼저 실행하세요.

```text
supabase/link_employee_accounts.sql
```

이 SQL을 실행해야 `employees.user_id` 컬럼과 RLS 정책, 승인 함수가 DB에 반영됩니다.

## 사용 흐름

1. 관리자가 직원 관리에서 직원명을 미리 등록합니다.
2. 사용자가 초대 코드로 참여 요청을 보냅니다.
3. 관리자가 설정 > 참여 요청에서 요청자의 이메일을 확인합니다.
4. “연결할 직원 선택”에서 해당 직원명을 선택합니다.
5. 승인합니다.
6. 이후 일반 사용자는 본인 직원명에 등록된 근무만 달력에 표시됩니다.

## 확인 결과

- `npm run build` 성공 확인했습니다.
- Vite의 500KB chunk 경고는 기존 번들 크기 관련 경고이며 빌드 실패는 아닙니다.
